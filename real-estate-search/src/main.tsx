import React from "react";
import ReactDOM from "react-dom/client";
import RealEstateSearch from "./RealEstateSearch.jsx";
import "./index.css"; // Подключаем TailwindCSS

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RealEstateSearch />
  </React.StrictMode>
);
